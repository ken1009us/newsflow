# List of the pip packages that must be installed for this project to work.
# (Travis CI will install these before it tries running your Doctests.)
newsapi-python
dash==2.2.0
dash-bootstrap-components
requests
IPython
nltk
pandas
numpy
seaborn
matplotlib
flask==2.0.3
werkzeug==2.0.3

# Required for deployment on Heroku
gunicorn==20.1.0
